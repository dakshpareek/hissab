name: queue-writer
description: Append a watcher-emitted signal to the shared durable queue.
inputs:
  - queue_path
  - signal
  - queue_manager_command
outputs:
  - queue_write_result
required_behaviors:
  - dedupe by signal identity
  - write structured queue items only
  - never touch case files
