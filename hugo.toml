baseURL = "https://hissab.daksh.be/"
languageCode = "en-in"
title = "Hissab"

[params]
  description = "India's accountability gap, documented."
  tagline = "We track what happens after the headlines fade."

[taxonomies]
  category = "categories"
  tag = "tags"
  state = "states"
  status = "statuses"

[outputs]
  home = ["HTML"]
  section = ["HTML"]
  taxonomy = ["HTML"]
  term = ["HTML"]

[pagination]
  pagerSize = 20
